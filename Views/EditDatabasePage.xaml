<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:AMK.Models.ViewModels"
             x:Class="AMK.Views.EditDatabasePage"
             Title="Сотрудники"
             BackgroundColor="{StaticResource AppBackground}">

    <Grid RowDefinitions="Auto,*">

        <!-- Top controls -->
        <Grid Padding="16,12" ColumnDefinitions="*,Auto" ColumnSpacing="12">
            <SearchBar Grid.Column="0"
                       Placeholder="Поиск по ФИО/логину"
                       Text="{Binding SearchText}"
                       SearchCommand="{Binding SearchCommand}"
                       BackgroundColor="White" />

            <Button Grid.Column="1"
                    Text="+"
                    Style="{StaticResource PrimaryButton}"
                    WidthRequest="44"
                    HeightRequest="44"
                    Command="{Binding NewUserCommand}" />
        </Grid>

        <!-- Users list -->
        <RefreshView Grid.Row="1"
                     IsRefreshing="{Binding IsLoading}"
                     Command="{Binding RefreshCommand}">

            <CollectionView ItemsSource="{Binding Users}"
                            SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Style="{StaticResource CardFrame}" Margin="16,8" Padding="12">
                            <Grid ColumnDefinitions="*,Auto,Auto" RowDefinitions="Auto,Auto" ColumnSpacing="10" RowSpacing="4">

                                <StackLayout Grid.Column="0" Grid.RowSpan="2" Spacing="2">
                                    <Label Text="{Binding FIO}" FontAttributes="Bold" FontSize="16" TextColor="{StaticResource TextPrimary}" />
                                    <Label Text="{Binding Login, StringFormat='Логин: {0}'}" FontSize="12" TextColor="{StaticResource Gray600}" />
                                    <Label Text="{Binding RoleDetail.Title, StringFormat='Роль: {0}'}" FontSize="12" TextColor="{StaticResource Gray600}" />
                                    <Label Text="{Binding DepartmentDetail.DepartmentName, StringFormat='Отдел: {0}'}" FontSize="12" TextColor="{StaticResource Gray600}" />
                                </StackLayout>

                                <Button Grid.Column="1" Grid.Row="0"
                                        Text="✏️"
                                        Style="{StaticResource SecondaryButton}"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:UsersViewModel}}, Path=EditUserCommand}"
                                        CommandParameter="{Binding .}" />

                                <Button Grid.Column="2" Grid.Row="0"
                                        Text="🗑️"
                                        Style="{StaticResource DangerButton}"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:UsersViewModel}}, Path=DeleteUserCommand}"
                                        CommandParameter="{Binding .}" />

                                <StackLayout Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1" Spacing="2">
                                    <Label Text="{Binding Email, StringFormat='📧 {0}'}" FontSize="12" TextColor="{StaticResource Gray500}" />
                                    <Label Text="{Binding Phone, StringFormat='📱 {0}'}" FontSize="12" TextColor="{StaticResource Gray500}" />
                                </StackLayout>

                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>

        <!-- Edit sheet -->
        <Grid IsVisible="{Binding IsEditing}" BackgroundColor="#80000000" Padding="16" RowDefinitions="*" ColumnDefinitions="*">
            <Frame Style="{StaticResource CardFrame}" Padding="16" VerticalOptions="Center" HorizontalOptions="Center" WidthRequest="340">
                <ScrollView>
                    <StackLayout Spacing="10">
                        <Label Text="{Binding SelectedUser.ID_user, StringFormat='Пользователь #{0}'}"
                               FontAttributes="Bold"
                               FontSize="16"
                               TextColor="{StaticResource TextPrimary}" />

                        <Entry Placeholder="ФИО*" Text="{Binding SelectedUser.FIO}" />
                        <Entry Placeholder="Логин*" Text="{Binding SelectedUser.Login}" />
                        <Entry Placeholder="Пароль*" Text="{Binding SelectedUser.Password}" IsPassword="True" />

                        <Entry Placeholder="Email" Text="{Binding SelectedUser.Email}" Keyboard="Email" />
                        <Entry Placeholder="Телефон" Text="{Binding SelectedUser.Phone}" Keyboard="Telephone" />

                        <DatePicker Date="{Binding SelectedUser.Birthday}" />

                        <Picker Title="Роль" ItemsSource="{Binding Roles}" ItemDisplayBinding="{Binding Title}" SelectedItem="{Binding SelectedRole}" />
                        <Picker Title="Отдел" ItemsSource="{Binding Departments}" ItemDisplayBinding="{Binding DepartmentName}" SelectedItem="{Binding SelectedDepartment}" />

                        <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                            <Button Grid.Column="0" Text="Отмена" Style="{StaticResource SecondaryButton}" Command="{Binding CancelEditCommand}" />
                            <Button Grid.Column="1" Text="Сохранить" Style="{StaticResource PrimaryButton}" Command="{Binding SaveUserCommand}" />
                        </Grid>

                    </StackLayout>
                </ScrollView>
            </Frame>
        </Grid>

    </Grid>
</ContentPage>
